<h6>Dashboard</h6>
<h3>Welcome {{user.displayName}}</h3>
<p>Last logged on {{user.lastLoggedOn}}</p>
{{#if journals}}
<p>Your diary entries...</p>
<table class="striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Title</th>
      <th>Created</th>
      <th>Updated</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {{#each journals}}
    <tr>
      <td>
        <a href="/journals/date/{{formatDate journalDate 'YYYYMMDD'}}">{{formatDate journalDate 'Do MMM, YYYY'}}</a>
      </td>
      <td><a href="/journals/{{id}}">{{title}}</a></td>
      <td>{{formatDate createdAt 'DD/MM/YYYY, HH:mm:ss z' true}}</td>
      <td>{{formatDate updatedAt 'DD/MM/YYYY, HH:mm:ss z' true}}</td>
      <td><span class="dash-status">{{status}}</span></td>
      <td></td>
    </tr>
    {{/each}}
  </tbody>
</table>
{{else}}
<p>
  You have no journal entry so far. Let's create one...
  <a href="/journals/add" class="btn-floating btn-large waves-effect waves-light red"><i class="fas fa-plus"></i></a>
</p>
{{/if}}
